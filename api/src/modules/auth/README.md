# auth

Fase 4. getAuthContext(): bearer token -> Supabase Auth -> company_users -> { userId, companyId, role }.
assertRole() por rota. Reaproveita o padrao de api/src/plugins/auth.ts do repo antigo (era a parte boa).
