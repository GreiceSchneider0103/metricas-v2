# components

Componentes compartilhados entre paginas (cards, filtros, tabelas). Portar so o que
for reaproveitavel do repo antigo (stat-card.tsx, filter-bar.tsx, status-pill.tsx sao bons candidatos).
